#include<iostream>
using namespace  std;
int main()
{
    int a;
    if(3+2/5)
    cout<<("this works");
    if(a=10)
    cout<<("even this works");
    if(0)
    cout<<("surprisingly even this works");
    return 0;}